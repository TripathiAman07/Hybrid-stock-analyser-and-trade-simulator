{
  "name": "quantyx-v4",
  "private": true,
  "version": "4.0.0",
  "type": "module",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "server": "cd backend && npm start",
    "dev:all": "concurrently -n \"backend,frontend\" -c \"green,cyan\" \"cd backend && npm start\" \"cd frontend && npm run dev\"",
    "build": "cd frontend && npm run build",
    "preview": "cd frontend && npm run preview",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install"
  },
  "dependencies": {
    "concurrently": "^9.0.0"
  }
}
